fn main() {
   let interest:f32 = 8.0; //automatic typcasting is not allowed in rust, therefore we must manually assign accurate data types in order for the code to run
   println!("interest is {}",interest );
}
