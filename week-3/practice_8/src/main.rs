fn main() {
    let fees = 25_000;
    println!("fees is {}",fees );
// by default, rust variables are immutable and cannot be freely changed.
    let Fees = 35_000; //change in variable to allow code to run
    println!("fees changed is {}",Fees );
}
